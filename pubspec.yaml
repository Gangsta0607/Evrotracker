name: evropochta_tracker
description: "Приложение для удобного отслеживания доставок Европочты"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  html: ^0.15.4
  
  webview_flutter: ^4.4.2
  shared_preferences: ^2.2.2
  shimmer: ^3.0.0
  intl: ^0.20.2
  provider: ^6.1.5
  dynamic_color: ^1.6.4


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
